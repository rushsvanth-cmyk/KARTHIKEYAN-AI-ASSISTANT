#include <iostream>
#include <string>
#include <algorithm> 
#include <cmath>
#include <ctime>
#include <sstream> 
#include <unistd.h> 

using namespace std;

int main() 
{
    string input;
    double num1, num2;
    char op;

    static string tasks[5]; 
    static int taskCount = 0;

    cout << "--- System Online (V4.6) ---" << endl;
    cout << "(Commands: hi, status, github, battery, list, exit)" << endl;

    while (true)
    {
        cout << "\nYou: ";
        getline(cin, input);

        if (input.empty()) continue;

        // --- 1. Automatic Math Detection ---
        if (isdigit(input[0])) 
        {
            stringstream ss(input);
            if (ss >> num1 >> op >> num2) 
            {
                cout << "Assistant (Math): ";
                switch(op) 
                {
                    case '+': cout << num1 + num2 << " ✅"; break;
                    case '-': cout << num1 - num2 << " ✅"; break;
                    case '*': cout << num1 * num2 << " ✅"; break;
                    case '/': 
                        if(num2 != 0) cout << num1 / num2 << " ✅";
                        else cout << "Error: Cannot divide by zero! ❌";
                        break;
                    case '^': cout << pow(num1, num2) << " 🔥"; break;
                    default: cout << "Unknown operator! 😅";
                }
                cout << endl;
                continue; 
            }
        }

        string lowerInput = input;
        transform(lowerInput.begin(), lowerInput.end(), lowerInput.begin(), [](unsigned char c){ return tolower(c); });

        // --- 2. System Commands ---
        if (lowerInput == "exit" || lowerInput == "quit") 
        {
            cout << "Assistant: Goodbye! Have a wonderful day. 🫂" << endl;
            break;
        }
        else if (lowerInput == "who is your owner" || lowerInput == "owner" || lowerInput == "creator") 
        {
            cout << "Assistant: I was developed and built by Karthikeyan. 🛠️" << endl;
        }
        else if (lowerInput == "date" || lowerInput == "time" || lowerInput == "today") 
        {
            time_t now = time(0);
            char* dt = ctime(&now);
            cout << "Assistant: The current date and time is: " << dt;
        }
        else if (lowerInput == "hi" || lowerInput == "hello") 
        {
            cout << "Assistant: Hello! I am your virtual assistant. How can I help you today?" << endl;
        }

        // --- 3. Tesla Battery Logic ---
        else if (lowerInput == "battery" || lowerInput == "range") 
        {
            double battery_percent;
            cout << "Assistant: What is your current battery percentage (0-100)? ";
            cin >> battery_percent;
            cin.ignore(1000, '\n'); 

            if (battery_percent >= 0 && battery_percent <= 100) 
            {
                double range = battery_percent * 5;
                cout << "Assistant: Estimated range is approximately " << range << " KM! 🚗🔋" << endl;
            } 
            else cout << "Assistant: Please enter a valid percentage. ❌" << endl;
        }

        // --- 4. Reminder Logic ---
        else if (lowerInput.find("remind me at") != string::npos)
        {
            string alarmTime = lowerInput.substr(13); 
            cout << "Assistant: Sure Karthikeyan, I will remind you at " << alarmTime << "! ⏰" << endl;
            
            bool reminded = false;
            while (!reminded) {
                time_t now = time(0);
                tm *ltm = localtime(&now);
                char currentTime[6];
                strftime(currentTime, sizeof(currentTime), "%H:%M", ltm);
                
                if (string(currentTime) == alarmTime)
                {
                    cout << "\n[REMINDER] 🔔 Karthikeyan, it is " << alarmTime << "! Time to start your work!" << endl;
                    reminded = true;
                }
                sleep(2); 
            }
        }

        // --- 5. Success Task List ---
        else if (lowerInput == "add task") {
            if (taskCount < 5) {
                cout << "Assistant: What task would you like to add?: ";
                string newTask;
                getline(cin, newTask);
                tasks[taskCount] = newTask;
                taskCount++;
                cout << "Assistant: Task added. ✅" << endl;
            } else cout << "Assistant: List is full! ❌" << endl;
        }
        else if (lowerInput == "show task" || lowerInput == "list" || lowerInput == "tasks") {
            cout << "\n--- Your Success Task List ---" << endl;
            if (taskCount == 0) cout << "No tasks yet. ✨" << endl;
            else {
                for (int i = 0; i < taskCount; i++) cout << i + 1 << ". " << tasks[i] << endl;
            }
        }

        // --- 6. Professional Shortcuts (MOVED INSIDE THE LOOP) ---
        else if (lowerInput == "github" || lowerInput == "git")  
        {
            cout << "Assistant: Opening your GitHub profile, Karthikeyan... 🚀" << endl;
            system("am start -a android.intent.action.VIEW -d https://github.com/rushsvanth-cmyk");
        }
        else if (lowerInput == "status" || lowerInput == "info")
        {
            cout << "\n--- Assistant Status ---" << endl;
            cout << "Version: 4.6" << endl;
            cout << "Developer: Karthikeyan" << endl;
            cout << "Career Goal: Tesla Engineer (by 2035) 🏎️" << endl;
            cout << "Today's Status: Progressing & Leveling Up... ✅" << endl;
            cout << "------------------------" << endl;
        }

        // Default Response
        else 
        {
            cout << "Assistant: I didn't quite catch that. Try 'hi', 'status', or 'battery'!" << endl;
        }
    } // <--- This brace closes the while(true) loop

    return 0; // <--- This returns from main()
}
