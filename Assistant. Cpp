#include <iostream>
#include <string>
#include <algorithm> 
#include <cmath>
#include <ctime>
#include <sstream> 
#include <unistd.h> 
#include <fstream> // ஃபைல் சேமிக்க இது புதுசு!

using namespace std;

int main() 
{
    string input;
    double num1, num2;
    char op;

    static string tasks[5]; 
    static int taskCount = 0;

    cout << "--- System Online (V5.2 - Professional) ---" << endl;
    cout << "(Commands: hi, status, write note, motivation, search for, exit)" << endl;

    while (true)
    {
        cout << "\nYou: ";
        getline(cin, input);
        if (input.empty()) continue;

        // 1. Automatic Math Detection
        if (isdigit(input[0])) 
        {
            stringstream ss(input);
            if (ss >> num1 >> op >> num2) 
            {
                cout << "Assistant (Math): ";
                switch(op) {
                    case '+': cout << num1 + num2 << " ✅"; break;
                    case '-': cout << num1 - num2 << " ✅"; break;
                    case '*': cout << num1 * num2 << " ✅"; break;
                    case '/': 
                        if(num2 != 0) cout << num1 / num2 << " ✅";
                        else cout << "Error! ❌";
                        break;
                    case '^': cout << pow(num1, num2) << " 🔥"; break;
                    default: cout << "Unknown operator! 😅";
                }
                cout << endl; continue; 
            }
        }

        string lowerInput = input;
        transform(lowerInput.begin(), lowerInput.end(), lowerInput.begin(), [](unsigned char c){ return tolower(c); });

        // 2. System Commands
        if (lowerInput == "exit" || lowerInput == "quit") {
            cout << "Assistant: Goodbye Karthikeyan! See you at Tesla. 🫂" << endl;
            break;
        }
        else if (lowerInput == "hi" || lowerInput == "hello") {
            cout << "Assistant: Hello creator! How can I help you build today?" << endl;
        }
        
        // 3. Dev Notepad (புதிய வசதி)
        else if (lowerInput == "write note") {
            ofstream outfile;
            outfile.open("karthi_notes.txt", ios_base::app);
            cout << "Assistant: என்ன குறிப்பு எழுதணும் நண்பா?: ";
            string note; getline(cin, note);
            outfile << note << endl;
            outfile.close();
            cout << "Assistant: குறிப்பு சேமிக்கப்பட்டது! 📝" << endl;
        }
        else if (lowerInput == "read note") {
            ifstream infile("karthi_notes.txt");
            string line;
            cout << "--- Your Dev Notes ---" << endl;
            if (infile.is_open()) {
                while (getline(infile, line)) cout << "- " << line << endl;
                infile.close();
            } else cout << "குறிப்புகள் எதுவும் இல்லை! ✨" << endl;
        }

        // 4. Motivation (புதிய வசதி)
        else if (lowerInput == "motivation") {
            string quotes[] = {
                "கார்த்திகேயன், 2035-ல் நீ ஒரு டெஸ்லா இன்ஜினியர்! 🏎️",
                "ஒவ்வொரு வரியும் ஒரு வெற்றி! கோடிங்கைத் தொடரு. 💻",
                "கடின உழைப்புக்கு எப்போதுமே பலன் உண்டு. 🔥",
                "உன் அசிஸ்டன்ட் உன்னை விட புத்திசாலி ஆகப்போகுது! 🚀"
            };
            srand(time(0));
            cout << "Assistant: " << quotes[rand() % 4] << endl;
        }

        // 5. Battery Logic
        else if (lowerInput == "battery" || lowerInput == "range") {
            double p; cout << "Assistant: Battery %? ";
            cin >> p; cin.ignore(1000, '\n'); 
            if (p >= 0 && p <= 100) cout << "Assistant: Range: " << p * 5 << " KM! 🚗🔋" << endl;
            else cout << "Assistant: Invalid! ❌" << endl;
        }

        // 6. Search & GitHub
        else if (lowerInput.find("search for") != string::npos) {
            string query = input.substr(11);
            string url = "https://www.google.com/search?q=" + query;
            string cmd = "am start -a android.intent.action.VIEW -d \"" + url + "\"";
            system(cmd.c_str());
        }
        else if (lowerInput == "github") {
            system("am start -a android.intent.action.VIEW -d https://github.com/rushsvanth-cmyk");
        }

        // 7. Status & Others
        else if (lowerInput == "status") {
            cout << "V5.2 | Dev: Karthikeyan | Target: Tesla 2035 🏎️" << endl;
        }
        else if (lowerInput == "who is karthikeyan") {
            cout << "Assistant: A future Tesla Legend and my creator! 🔥" << endl;
        }
        else {
            cout << "Assistant: புரியவில்லை! 'status' அல்லது 'motivation' என்று டைப் செய்யவும்." << endl;
        }
    }
    return 0; 
}

