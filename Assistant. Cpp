#include <iostream>
#include <string>
#include <algorithm> 
#include <cmath>
#include <ctime>
#include <sstream> 
#include <unistd.h> 
#include <fstream> 

using namespace std;

int main() 
{
    string input;
    double num1, num2;
    char op;

    static string tasks[5]; 
    static int taskCount = 0;

    cout << "--- System Online (V5.7 - Schedule Optimizer) ---" << endl;
    cout << "(Commands: hi, status, battery, charge, model, schedule, weather, write note, today, exit)" << endl;

    while (true)
    {
        cout << "\nYou: ";
        getline(cin, input);
        if (input.empty()) continue;

        string lowerInput = input;
        transform(lowerInput.begin(), lowerInput.end(), lowerInput.begin(), [](unsigned char c){ return tolower(c); });

        // 1. Automatic Math Detection
        if (isdigit(input[0])) 
        {
            stringstream ss(input);
            if (ss >> num1 >> op >> num2) 
            {
                cout << "Assistant (Math): ";
                switch(op) {
                    case '+': cout << num1 + num2 << " ✅"; break;
                    case '-': cout << num1 - num2 << " ✅"; break;
                    case '*': cout << num1 * num2 << " ✅"; break;
                    case '/': 
                        if(num2 != 0) cout << num1 / num2 << " ✅";
                        else cout << "Error! Cannot divide by zero. ❌";
                        break;
                    case '^': cout << pow(num1, num2) << " 🔥"; break;
                    default: cout << "Unknown operator! 😅";
                }
                cout << endl; continue; 
            }
        }

        // 2. System Commands
        if (lowerInput == "exit" || lowerInput == "quit") {
            cout << "Assistant: Goodbye Karthikeyan! Heading to Tesla HQ... 🏎️🫂" << endl;
            break;
        }
        else if (lowerInput == "hi" || lowerInput == "hello") {
            cout << "Assistant: Hello creator! Ready to build the future today?" << endl;
        }
        
        // 3. Dev Notepad
        else if (lowerInput == "write note") {
            ofstream outfile;
            outfile.open("notes.txt", ios_base::app);
            cout << "Assistant: What should I write down?: ";
            string note; getline(cin, note);
            outfile << note << endl;
            outfile.close();
            cout << "Assistant: Note saved to memory! 📝" << endl;
        }
        else if (lowerInput == "read note") {
            ifstream infile("notes.txt");
            string line;
            cout << "--- Your Dev Notes ---" << endl;
            if (infile.is_open()) {
                while (getline(infile, line)) cout << "- " << line << endl;
                infile.close();
            } else cout << "No notes found! ✨" << endl;
        }

        // 4. Motivation
        else if (lowerInput == "motivation") {
            string quotes[] = {
                "Karthikeyan, the stage at Tesla 2035 is waiting for you! 🏎️",
                "Every line of code is a step toward success. Keep going! 💻",
                "Hard work always pays off. Stay focused! 🔥",
                "Your assistant is getting smarter every day, just like you! 🚀"
            };
            srand(time(0));
            cout << "Assistant: " << quotes[rand() % 4] << endl;
        }

        // 5. Special Day Reminder
        else if (lowerInput == "add event") {
            string date, eventName;
            cout << "Assistant: Enter the date to remember (DD/MM): ";
            cin >> date;
            cout << "Assistant: What is the occasion?: ";
            cin.ignore();
            getline(cin, eventName);
            
            ofstream eventFile;
            eventFile.open("events.txt", ios_base::app);
            eventFile << date << " " << eventName << endl;
            eventFile.close();
            cout << "Assistant: Noted! I'll remember " << eventName << " on " << date << ". ✅" << date;
        }
        else if (lowerInput == "check events" || lowerInput == "today") {
            time_t now = time(0);
            tm *ltm = localtime(&now);
            char curDate[6];
            strftime(curDate, sizeof(curDate), "%d/%m", ltm); 

            ifstream eventFile("events.txt");
            string fileDate, fileEvent;
            bool found = false;

            cout << "Assistant: Checking your schedule..." << endl;
            while (eventFile >> fileDate) {
                getline(eventFile, fileEvent);
                if (fileDate == string(curDate)) {
                    cout << "🔔 [Special Event Today!]:" << fileEvent << " 🚀" << endl;
                    found = true;
                }
            }
            if (!found) cout << "Assistant: Nothing special on the calendar for today, Karthikeyan! ✨" << endl;
            eventFile.close();
        }

        // 6. Tesla Battery Core Logic
        else if (lowerInput == "battery") {
            double p; cout << "Assistant: Battery %? "; cin >> p; cin.ignore();
            if (p >= 0 && p <= 100) cout << "Assistant: Estimated Range: " << p * 5 << " KM! 🚗🔋" << endl;
            else cout << "Assistant: Please enter a percentage between 0 and 100! ❌" << endl;
        }
        else if (lowerInput == "charge") {
            double current_pct;
            cout << "Assistant: Current battery percentage (0-100)?: ";
            cin >> current_pct;
            cin.ignore();
            
            if (current_pct >= 0 && current_pct < 100) {
                double needed = 100.0 - current_pct;
                double home_rate = 0.12;       
                double supercharger_rate = 2.0; 
                
                cout << "\n--- Charging Estimates to 100% ---" << endl;
                cout << "⚡ Tesla Home Wall Connector: Approx. " << (needed / home_rate) / 60 << " hours." << endl;
                cout << "⚡ Tesla Supercharger V3: Approx. " << needed / supercharger_rate << " minutes." << endl;
            } else if (current_pct == 100) {
                cout << "Assistant: Battery is already fully charged! Pack is ready to roll. 🏎️⚡" << endl;
            } else {
                cout << "Assistant: Invalid calculation data. ❌" << endl;
            }
        }

        // 7. Tesla Model Fleet Customizer
        else if (lowerInput == "model" || lowerInput == "tesla car") {
            int choice;
            cout << "\n--- Select Tesla Model ---" << endl;
            cout << "1. Model S (Plaid)\n2. Model 3 (Long Range)\n3. Model X (Plaid)\n4. Model Y (Performance)" << endl;
            cout << "Enter choice (1-4): ";
            cin >> choice;
            cin.ignore();

            cout << "\n--- Vehicle Performance Profile ---" << endl;
            if (choice == 1) {
                cout << "🚗 Model: Tesla Model S Plaid\n🔋 Est. Range: 637 KM\n⚡ 0-100 KM/H: 2.1 Seconds 🔥" << endl;
            } else if (choice == 2) {
                cout << "🚗 Model: Tesla Model 3 Long Range\n🔋 Est. Range: 576 KM\n⚡ 0-100 KM/H: 4.2 Seconds" << endl;
            } else if (choice == 3) {
                cout << "🚗 Model: Tesla Model X Plaid\n🔋 Est. Range: 536 KM\n⚡ 0-100 KM/H: 2.6 Seconds" << endl;
            } else if (choice == 4) {
                cout << "🚗 Model: Tesla Model Y Performance\n🔋 Est. Range: 488 KM\n⚡ 0-100 KM/H: 3.6 Seconds" << endl;
            } else {
                cout << "Assistant: Invalid selection! ❌" << endl;
            }
        }

        // --- 8. NEW: Balanced Schedule Optimizer (V5.7) ---
        else if (lowerInput == "schedule" || lowerInput == "routine") {
            cout << "\n--- 🎯 Karthikeyan's Daily Balance Routine ---" << endl;
            cout << "🌅 Morning (Discipline & Fitness):" << endl;
            cout << "   - 06:00 AM to 07:30 AM -> RSS Shakha Training (Physical & Leadership Drill)" << endl;
            cout << "💻 Afternoon/Evening (Core Engineering Goal):" << endl;
            cout << "   - 04:00 PM to 06:00 PM -> C++ Advanced Coding & Algorithm Practice" << endl;
            cout << "   - 06:00 PM to 07:00 PM -> Project Building & GitHub Commit 🟩" << endl;
            cout << "🧠 Developer Reminder:" << endl;
            cout << "   - RSS gives you the discipline. C++ gives you the skill. Balance is key!" << endl;
        }

        // 9. Environment & Weather
        else if (lowerInput == "weather" || lowerInput == "climate") {
            cout << "Assistant: Opening the local weather report for Chennai... ☀️" << endl;
            system("am start -a android.intent.action.VIEW -d \"https://www.google.com/search?q=weather+chennai\"");
        }
        else if (lowerInput == "hydrate" || lowerInput == "health") {
            cout << "Assistant: Karthikeyan, drink some water to keep your brain fueled up! 💧" << endl;
        }

        // 10. Status & Profile Links
        else if (lowerInput == "status") {
            cout << "V5.7 | Dev: Karthikeyan | Routine: Balanced | Goal: Tesla Engineer 2035 🏎️" << endl;
        }
        else if (lowerInput == "github" || lowerInput == "git") {
            system("am start -a android.intent.action.VIEW -d https://github.com/rushsvanth-cmyk");
        }
        else {
            cout << "Assistant: Sorry, I didn't get that. Try 'status', 'schedule', or 'model'!" << endl;
        }
    } 
    return 0; 
}

